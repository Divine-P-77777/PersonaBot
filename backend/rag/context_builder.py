"""
Context builder for LLM prompts.
Combines persona config, retrieved chunks, and chat history into a prompt.
"""


# TODO: Month 3 implementation
def build_context(persona_config: dict, chunks: list[str], chat_history: list[dict]) -> str:
    """Build the full prompt context for LLM."""
    pass
